update the rdpmc code, plus move the new perf_event header in

merge into the perf_event tests tree?


check context switch results and see if those are high latency cases



compare kernels?  
  gcc version
  NMI watchdog (LOCKUP detection)
  frequency scaling

